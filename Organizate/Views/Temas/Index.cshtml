@model IEnumerable<Organizate.Tema>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div id="page-wrapper">
	<div class="row">
		<div class="col-lg-12">
			<h1 class="page-header">Temas</h1>
		</div>
		<!-- /.col-lg-12 -->
	</div>
	<p>
		@Html.ActionLink("Create New", "Create")
	</p>
	<table class="table">
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.tema_nombre)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Profesor_Materia.pro_mat_id)
			</th>
			<th></th>
		</tr>

		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.tema_nombre)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Profesor_Materia.pro_mat_id)
				</td>
				<td>
					@Html.ActionLink("Edit", "Edit", new { id = item.tema_id }) |
					@Html.ActionLink("Details", "Details", new { id = item.tema_id }) |
					@Html.ActionLink("Delete", "Delete", new { id = item.tema_id })
				</td>
			</tr>
		}

	</table>
	</div>